enum PermissionType { photos, video, audio, camera, notification, storage }

enum PermissionCurrentStatus { granted, denied, restricted, permanentlyDenied, limited, provisional }

enum OutputPermissionStatus { unknown, granted, denied }
